git clone (url)
git add (file names)
git commit -m "(committed message) #commit to local 
git push -u origin main #push to git hub
git remote add origin main(url)
git fetch (url) #to move an existing repo into your
git init (url) #to create a repo
git pull (url) 

